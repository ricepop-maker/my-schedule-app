# 개인 일정 스케줄 관리 앱 설계서

## 1. 개요

- **목적**: 개인용 일정 관리 웹앱. PC/안드로이드 등 여러 기기에서 로그인해서 같은 일정 데이터를 보고 씀. 안드로이드에서는 PWA로 홈 화면에 설치해서 앱처럼 사용
- **저장 방식**: Firebase Firestore (클라우드 DB, 무료 티어) — 로그인한 계정 기준으로 데이터 저장/동기화
- **인증**: Firebase Authentication, 이메일/비밀번호 로그인 + 구글 로그인 (계정 1개로 PC/폰 어디서든 동일한 일정 데이터 접근)
- **기술 스택**: 순수 HTML / CSS / JavaScript (프레임워크·빌드 도구 없음) + Firebase SDK(CDN, ES 모듈) + PWA(manifest, service worker)
- **폴더 위치**: `testapp/`

## 1.1 안드로이드 배포 방향 (2026-08-19 결정)

최종 목표는 안드로이드 APK지만, 현재 PC에 Java/Node.js/Android SDK가 전혀 설치되어 있지 않음. 아래 단계로 진행하기로 함:

1. **1단계 (지금)**: 순수 웹앱으로 완성 + PWA 설정 추가 (manifest.json, service worker, 아이콘) → 안드로이드 크롬에서 "홈 화면에 추가"로 앱처럼 설치 가능
2. **2단계 (추후, 필요시)**: 진짜 `.apk` 파일이 필요해지면 그때 Capacitor(WebView 래핑) 또는 TWA 방식으로 전환 — Node.js/Android Studio/SDK 설치 필요

**⚠️ 제약사항**: PWA 설치(홈 화면 추가, 서비스워커 등록)는 `file://`로 직접 여는 방식으로는 동작하지 않음. 반드시 http(s)로 서빙되어야 함 (예: 로컬 서버 실행 후 같은 와이파이의 폰에서 접속, 또는 GitHub Pages 등에 배포). 기존 CLAUDE.md의 "index.html을 브라우저로 직접 열기" 방식은 PC에서 기능 확인용으로는 계속 가능하지만, 안드로이드 설치 테스트 시에는 별도 서빙이 필요함.

## 1.2 저장소/인증 방향 (2026-08-19 결정, 2026-08-19 실제 연동 완료)

`localStorage` 단독 방식에서 **Firebase(Firestore + Authentication)**로 변경. 이유: 기기 간 동기화, 브라우저 데이터 삭제 시 유실 방지.

- **DB**: Firebase Firestore (무료 Spark 플랜 — 저장 1GiB, 읽기 5만/일, 쓰기 2만/일 등, 개인용 앱에 충분)
- **인증**: 이메일/비밀번호 로그인 + 구글 로그인(팝업 방식, `signInWithPopup`) 둘 다 지원. 어느 방식으로 로그인해도 같은 Firebase 프로젝트의 Authentication에 계정으로 등록됨. 로그인 전에는 앱 사용 불가 (일정 데이터가 계정에 종속)
- 구글 로그인을 쓰려면 Firebase 콘솔 > Authentication > Sign-in method에서 "Google" 제공업체를 추가로 활성화해야 함 (사용자가 직접 진행)
- **호스팅**: Firebase Hosting(무료)으로 배포 → https 주소가 생기므로 [1.1](#11-안드로이드-배포-방향-2026-08-19-결정)에서 언급한 "PWA는 file://로 설치 불가" 문제도 함께 해결됨
- **오프라인 동작**: `db.enablePersistence()`로 Firestore 오프라인 캐시 활성화. 네트워크 끊겨도 마지막으로 받은 데이터는 조회 가능하고 재연결 시 자동 동기화됨
- **SDK 로딩 방식 — 모듈(ES module) 대신 호환(compat) 버전 채택**: 처음엔 최신 모듈형 SDK(`import`)를 검토했으나, `<script type="module">`로 우리 자신의 로컬 JS 파일(`auth.js`/`app.js`)을 불러오면 Chrome이 `file://`에서 CORS 오류를 내며 막아버림 — 즉 파일을 더블클릭해서 여는 기존 방식이 완전히 깨짐. 대신 Firebase가 함께 제공하는 **호환(compat) SDK**(`firebase-app-compat.js` 등, 전역 `firebase` 객체 방식)를 일반 `<script src="...">`로 로드해서, `file://`로 바로 열어도 그대로 동작하도록 함. CDN에서 로드하므로 빌드 도구는 여전히 불필요
- `firebase-config.js`는 ES 모듈이 아닌 일반 스크립트로 변경 (`export` 제거, `firebaseConfig`를 전역 변수로 노출)

## 1.3 공휴일 데이터 소스 결정 (2026-08-19)

당초 "네이버 달력 API" 연동을 요청받았으나, **네이버는 개발자용 공휴일 조회 API를 공개하지 않음**을 확인 (조사 결과, 문의는 있었으나 정식 공개 API 없음). 대안 검토 결과:

- **공공데이터포털(data.go.kr) 특일정보 API**: 정식 국내 공휴일 API지만 CORS 미지원 → 브라우저에서 직접 호출 불가, 별도 백엔드/프록시(Firebase Cloud Functions 등) 필요 → 기각 (빌드 도구·백엔드 없는 현재 구조와 안 맞음, 요금제 이슈도 있음)
- **[Nager.Date](https://date.nager.at) API**: 채택. 무료, API 키 불필요, CORS 지원 직접 확인함(`access-control-allow-origin: *`), 설날/추석 등 음력 공휴일도 자동 계산되어 한글 이름과 함께 제공됨. 순수 프론트엔드 구조에 그대로 맞음
- 연도 단위로 조회해 메모리에 캐싱 (`GET https://date.nager.at/api/v3/PublicHolidays/{year}/KR`), 조회 실패 시 상태 메시지로 안내하고 빈 결과로 캐싱해 반복 재요청 방지
- **대체공휴일 이름 보정**: Nager.Date는 대체공휴일의 실제 날짜(예: 2026년 광복절 대체공휴일 8/17)는 정확히 계산해 주지만, 이름은 원래 명칭("광복절")만 주고 "대체공휴일" 표시를 붙여주지 않음. 양력 고정 공휴일(새해/3·1절/어린이날/현충일/제헌절/광복절/개천절/한글날/크리스마스)의 원래 날짜를 코드에 갖고 있다가, API가 준 날짜가 원래 날짜와 다르면 "대체공휴일(광복절)"처럼 자동으로 이름을 보정함

## 2. 화면 구성

### 2.1 레이아웃

```
+--------------------------------------------------+
|  헤더: 앱 제목 + [캘린더] [리스트] 뷰 전환 탭        |
+--------------------------------------------------+
|  툴바(중앙 정렬): <  2026년 8월  >   [+ 일정 추가]  [카테고리 필터] |
+--------------------------------------------------+
|                                                    |
|   캘린더 뷰                    또는     리스트 뷰       |
|   (월간 달력, 날짜별                (날짜순 정렬된       |
|    일정 점/태그 표시)                일정 카드 목록)     |
|                                                    |
+--------------------------------------------------+
```

### 2.2 캘린더 뷰

- 월 단위 그리드(7열 x 최대 6행)
- 각 날짜 셀에 해당 날짜의 일정을 카테고리 색상 점 또는 소형 태그로 표시 (최대 3개 노출, 초과 시 "+N")
- 날짜 셀 클릭(빈 영역) → 해당 날짜의 일정 목록을 하단/사이드 패널에 표시
- 날짜 셀 안의 개별 일정 칩 클릭 → 날짜 패널을 거치지 않고 바로 해당 일정의 수정 모달을 오픈 (리스트 뷰 카드 클릭과 동일한 동작)
- 일정 칩에 마우스 호버 시 제목/날짜·시간/카테고리/우선순위/메모 요약이 담긴 미리보기 툴팁이 커서 근처에 표시됨 (클릭하지 않아도 내용 확인 가능)
- 완료된 회차는 칩에 취소선 + 흐리게 표시 (반복 일정은 회차 단위로 판단 — 같은 일정이라도 날짜별로 완료 표시가 다를 수 있음)
- 좌우 화살표로 월 이동, "오늘" 버튼으로 현재 월 복귀
- 오늘 날짜는 강조 표시
- **요일 색상**: 토요일 날짜 숫자는 파랑, 일요일 날짜 숫자는 빨강 (요일 헤더 행의 "토"/"일" 라벨도 동일하게 색상 적용)
- **대한민국 공휴일 표시**: 공휴일 날짜는 (요일과 무관하게) 숫자가 빨강으로 표시되고, 날짜 아래에 공휴일 이름이 작게 함께 표시됨. [Nager.Date](https://date.nager.at) 공개 API에서 연도 단위로 조회해 캐싱 (무료, API 키 불필요, CORS 지원 확인됨, 음력 공휴일인 설날/추석도 자동 계산되어 제공됨)

### 2.3 리스트 뷰

- 날짜/시간순으로 정렬된 일정 카드 목록
- 카드에 표시: 제목, 날짜/시간, 카테고리 색상, 우선순위 색상 배지(낮음/보통/높음), 반복 여부 아이콘, 알림 아이콘, 완료 체크박스
- 카드 호버 시 캘린더와 동일한 요약 미리보기 툴팁 표시
- 완료 체크박스를 누르면 모달을 열지 않고 바로 **그 회차만** 완료 상태 토글 (제목에 취소선 + 카드 흐리게 처리). 클릭 즉시 반영되지 않고 "완료로 표시하시겠습니까?" 확인 팝업을 거친 후에만 적용됨 (취소 시 체크 상태 원복). 반복 일정이어도 다른 회차에는 영향 없음
- 지난 일정은 흐리게(dimmed) 처리
- 카테고리 필터로 목록 좁히기 가능

### 2.4 로그인 화면

- 앱 진입 시 로그인 상태가 아니면 로그인/회원가입 화면을 먼저 표시 (캘린더/리스트/툴바 등 본 화면은 숨김)
- 입력 필드: 이메일, 비밀번호
- 버튼: 로그인 / 회원가입(최초 1회, 계정이 없으면 새로 생성) / **구글로 로그인** (이메일/비밀번호 입력 없이 구글 계정으로 바로 로그인·가입) / 로그아웃(로그인 후 헤더 우측에 표시)
- 로그인 성공 시 해당 계정의 Firestore 데이터를 불러와 본 화면 표시
- Firebase Auth 에러(잘못된 비밀번호, 이미 존재하는 이메일 등)를 한글 메시지로 변환해 표시
- **최근 로그인 방법 표시**: 로그인에 성공하면 사용한 방식(`email` 또는 `google`)을 `localStorage`에 기록해두고, 다음 방문 시 로그인 화면에서 그 방식의 버튼 옆에 "최근에 사용함" 배지를 표시 (기기별 UI 편의 기능이라 Firestore가 아닌 localStorage에 저장)
- **⚠️ file://에서 구글 로그인 팝업 제약**: 이메일/비밀번호 로그인은 `file://`로 직접 열어도 대체로 동작하지만, 구글 로그인(`signInWithPopup`)은 팝업-원본 창 간 통신에 정상적인 http(s) origin이 필요해서 `file://`에서는 실패할 가능성이 높음. 구글 로그인까지 테스트하려면 로컬 서버(예: `python -m http.server`)나 Firebase Hosting으로 서빙해서 접속해야 함

### 2.5 일정 추가/수정 모달(팝업)

- "+ 일정 추가" 버튼 → 처음부터 입력 가능한 **편집 모드**로 모달 오픈
- 기존 일정 클릭(캘린더 칩 / 리스트 카드 / 날짜상세 카드) → **읽기 전용(보기) 모드**로 모달 오픈. 모든 입력 필드가 비활성화되고, 하단에는 "수정" 버튼만 표시되어 바로 삭제/수정할 수 없음
  - "수정" 버튼을 눌러야 필드가 활성화되며 저장/삭제/취소 버튼이 있는 편집 모드로 전환됨
  - 편집 모드에서 "취소"를 누르면 변경사항을 버리고 원래 값 그대로 다시 보기 모드로 돌아감 (모달이 닫히지 않음)
  - 새 일정 추가 중 "취소"는 모달을 완전히 닫음
- 폼 맨 위, fieldset 바깥에 완료 관련 컨트롤 배치 (다른 필드들과 달리 보기 모드에서도 항상 조작 가능 — "수정" 버튼 없이 바로 가능):
  - **완료 여부** 체크박스: 지금 보고 있는 **이 회차 하나만** 완료 처리 (반복 일정이어도 다른 회차엔 영향 없음). 기존 일정이면 토글 시 확인 팝업을 거쳐 즉시 반영, 새 일정 추가 중이면 저장 시점에 반영됨
  - **전체 완료 버튼**: 반복 있는 일정에서만 표시됨(반복 없음이면 숨김). 누르면 확인 팝업 후 해당 반복 일정의 모든 회차를 한 번에 완료 처리 — 반복 종료일이 있으면 그때까지, 없으면(무한) 시작일로부터 1년치까지만 적용. 이미 전체 완료 상태면 버튼이 "전체 완료 해제"로 바뀌어 한 번에 되돌릴 수 있음
- 입력 필드 (기존 순서 유지):
  - 제목 (필수)
  - 날짜 (필수) — 브라우저 기본 날짜 입력 대신 **앱 디자인과 통일된 커스텀 미니 달력**을 클릭 시 팝업으로 표시. 월 이동 가능, 오늘/선택된 날짜 강조, 토요일=파랑·일요일=빨강, 공휴일도 메인 캘린더와 동일하게 빨강+이름 표시
  - 시작 시간 / 종료 시간 (선택, 종일 일정 체크박스)
  - 카테고리/색상 태그 (검색형 콤보박스 — 입력해서 필터링 후 선택, 카테고리 많아져도 빠르게 찾기)
  - 우선순위 (낮음 / 보통 / 높음, 레벨별 고정 색상: 낮음=초록 · 보통=노랑 · 높음=빨강. 캘린더/리스트에 색상 배지로 표시)
  - 메모/상세설명 (textarea)
  - 반복 설정 (**기본값 "없음"** / 매일 / 매주 / 매월 / 주중 / 요일선택)
  - 반복 종료 (반복 설정을 켜면 표시됨): **기본값은 "무한"**(종료일 없음, 계속 반복). 프리셋 버튼으로 [무한 / 1주일 / 한달 / 1년 / 직접 선택] 중 선택 — 1주일·한달·1년은 시작 날짜 기준으로 자동 계산된 종료일을 표시(읽기 전용), "직접 선택"을 고르면 날짜를 임의로 입력 가능. 반복 설정을 새로 켤 때마다 무한으로 초기화됨
  - 알림 설정 (없음 / 정시 / 5분 전 / 10분 전 / 30분 전 / 1시간 전 / 1일 전)
- 저장 / 삭제 / 취소 버튼
- 모달이 열릴 때 배경이 페이드인되고 모달 자체는 살짝 확대되며 나타나는 등장 애니메이션 적용 (닫을 때도 역방향으로 페이드아웃)

## 3. 데이터 모델

Firestore에 컬렉션으로 저장. 사용자별 데이터 격리를 위해 로그인한 사용자의 `uid` 하위에 저장:

```
users/{uid}/schedules/{scheduleId}   — 일정 문서
users/{uid}/categories/{categoryId}  — 카테고리 문서
```

**schedules 문서**

```json
{
  "title": "일정 제목",
  "date": "2026-08-20",
  "startTime": "14:00",
  "endTime": "15:00",
  "allDay": false,
  "categoryId": "카테고리 문서 ID",
  "priority": "low | medium | high (기본값 medium)",
  "doneDates": "[\"2026-08-20\"] 형태의 날짜 배열 (완료 처리된 회차들의 날짜, 기본값 []). 반복 없는 일정은 최대 1개(자기 date)만 들어감",
  "memo": "상세 설명 텍스트",
  "repeat": "none | daily | weekly | monthly | weekday | custom",
  "repeatEndDate": "2026-12-31 또는 빈 문자열 (반복 종료일. 빈 문자열=무한 반복이 기본값)",
  "customDays": "[0,2,4] (repeat이 custom일 때만 사용. 0=일 ~ 6=토)",
  "alarm": "none | onTime | 5min | 10min | 30min | 1hour | 1day",
  "ownerId": "생성한 사용자 uid (그룹 기능 대비 예약 필드, 현재는 미리보기용 임시값 \"me\")",
  "groupId": "null 이면 개인 일정, 값이 있으면 해당 그룹에 공유된 일정 (그룹 기능 대비 예약 필드, 아직 UI 없음)",
  "createdAt": "Firestore Timestamp"
}
```

**categories 문서**

```json
{
  "name": "업무",
  "color": "#4dabf7"
}
```

- 문서 ID는 Firestore가 자동 생성 (`addDoc`), 별도 id 필드 불필요
- 기본 카테고리 3~4개(업무/개인/약속/기타)는 최초 로그인(회원가입) 시 자동으로 생성해 넣어줌
- 반복 일정은 마스터 문서 1개만 저장하고, 화면 렌더링 시점에 반복 규칙을 펼쳐서(expand) 표시 (데이터 중복 저장 방지, [기존 방식](#7-상태-관리-mainjs-내부-구조-개략) 유지)
- 반복 종류: `daily`(매일) / `weekly`(매주, 시작일과 같은 요일) / `monthly`(매월, 같은 날짜) / `weekday`(주중, 월~금만) / `custom`(요일 직접 선택, `customDays`에 저장된 요일에만 생성)
- Firestore 보안 규칙: `request.auth.uid == uid` 인 경우에만 해당 사용자의 `users/{uid}/**` 문서 읽기/쓰기 허용, 그 외 모두 거부

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{uid}/{document=**} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
```

⚠️ 위 규칙은 아직 Firebase 콘솔에 실제로 반영되지 않음 — 처음 Firestore를 만들 때 "테스트 모드"(30일간 전체 공개)로 시작했으므로, **Firestore Database > 규칙 탭에서 위 내용으로 직접 교체해야 함** (Claude가 대신 할 수 없는 콘솔 작업)
- **⚠️ 그룹 기능 관련 미해결 사항**: 지금의 `users/{uid}/schedules` 구조는 "일정은 항상 만든 사람 소유"를 전제로 함. 그룹 일정처럼 여러 사용자가 같이 봐야 하는 경우엔 이 구조로는 안 맞아서, 실제 그룹 기능을 만들 때는 `groups/{groupId}/schedules` 같은 별도 컬렉션이나 공유 참조 방식을 다시 설계해야 함. `groupId` 필드는 우선 자리만 잡아둔 상태 ([9.1](#91-기본-확장) 참고)

## 4. 주요 기능 명세

| 기능 | 설명 |
|---|---|
| 로그인/회원가입/로그아웃 | Firebase Authentication (이메일/비밀번호 + 구글 로그인) |
| 일정 CRUD | 추가, 조회, 수정, 삭제 (Firestore) |
| 캘린더/리스트 전환 | 탭 클릭으로 뷰 전환, 마지막 선택 뷰 기억(localStorage — 이 값은 기기별 UI 설정이라 계속 로컬에 둠) |
| 월 이동 | 이전/다음 달, 오늘로 이동 |
| 반복 일정 | 매일/매주/매월/주중/요일선택 반복 생성, 종료는 기본 무한 + 1주일/한달/1년/직접선택 프리셋 |
| 카테고리 필터 | 특정 카테고리만 표시/숨김 |
| 카테고리 검색 | 일정 모달 내 카테고리 선택을 검색형 콤보박스로 제공 |
| 우선순위 | 낮음/보통/높음 3단계, 레벨별 고정 색상으로 캘린더·리스트에 표시 |
| 완료 처리 | 회차 단위로 완료 표시/해제(완료 시 취소선+흐리게), 반복 일정은 "전체 완료"로 모든 회차 일괄 처리 가능 |
| 일정 검색 | 제목·메모·카테고리명 기준으로 전체 일정(월 무관) 검색, 검색 중엔 자동으로 리스트뷰에 결과 표시 |
| JSON 내보내기/가져오기 | 헤더의 "데이터 관리" 버튼으로 여는 전용 팝업에서 진행. 내보내기는 전체 일정+카테고리를 JSON 파일로 다운로드, 가져오기는 파일 선택 시 기존 일정에 추가 병합(카테고리는 이름 같으면 재사용, id 충돌 시 새 id 발급) |
| 다크 모드 | 헤더의 토글 버튼으로 전환, localStorage에 저장해 다음 방문 시에도 유지, 로그인 화면에도 동일하게 적용 |
| 드래그 앤 드롭 이동 | 캘린더에서 일정 칩을 다른 날짜 칸으로 드래그하면 날짜 변경. **반복 없는 일정만 가능** (반복 일정은 특정 회차만 옮기는 게 데이터 모델상 불가해 제외) |
| 브라우저 알림 | Notification API로 설정된 시각에 알림 표시 (권한 요청 필요) |
| 데이터 영속성/동기화 | Firestore 자동 저장 + 실시간 동기화(`onSnapshot`), 오프라인 캐시 지원 |

## 5. 알림(Notification) 동작 방식

- 앱 최초 접속 시 브라우저 알림 권한 요청
- 앱이 열려 있는 동안 주기적으로(예: 1분마다) 다가오는 일정을 체크하여 알림 시각이 되면 `Notification` 객체로 알림 표시
- 브라우저 알림 특성상 **앱 탭이 열려 있어야 알림이 동작** (Service Worker 미사용, 순수 정적 페이지 제약사항으로 설계서에 명시)

## 6. 파일 구조

```
testapp/
├── index.html       # 로그인/회원가입 화면 전용 페이지 (진입점)
├── app.html          # 앱 본 화면 (헤더, 툴바, 캘린더/리스트 컨테이너, 모달) — 로그인 성공 후 이동하는 페이지
├── style.css         # 전체 스타일 (레이아웃, 캘린더 그리드, 모달, 색상 팔레트) — 두 페이지 공용
├── auth.js           # index.html 전용 로직 (Firebase Authentication으로 로그인/회원가입/구글 로그인, 성공 시 app.html로 이동)
├── app.js            # app.html 전용 로직 (Firestore 실시간 연동, 렌더링, 모달, 캘린더 등 나머지 전체)
├── firebase-config.js # Firebase 프로젝트 설정값(apiKey 등) — 별도 파일로 분리해 관리
├── manifest.json     # PWA 설치를 위한 앱 메타데이터 (이름, 아이콘, 테마색, standalone 모드)
├── sw.js             # 서비스 워커 (오프라인 캐싱, PWA 설치 요건 충족용)
└── icons/            # PWA 아이콘 (192x192, 512x512)
```

- 기존 프로젝트 컨벤션과 동일하게 이 폴더 안에서 독립적으로 완결되는 구조 (다른 폴더와 공유 코드 없음)
- `manifest.json`/`sw.js`/`icons/`/`firebase-config.js`는 이번에 새로 추가되는 부분 (기존 프로젝트들엔 없던 구성)
- **페이지 분리(2026-08-19)**: 원래 `index.html` 한 파일에 로그인 화면과 앱 본 화면을 모두 넣고 JS로 보이기/숨기기 전환했으나, 로그인 화면(`index.html`)과 앱 본 화면(`app.html`)을 별도 HTML 페이지로 분리함. 로그인 성공 시 `app.html`로 `location.href` 이동, 로그아웃 시 반대로 이동. 지금은(미리보기 단계) `localStorage`의 `mockLoggedIn` 플래그로 로그인 여부를 흉내내고 있고, `app.html`을 로그인 없이 직접 열면 `index.html`로 되돌아감. Firebase Authentication 연동 후에는 이 부분이 실제 인증 상태 체크로 교체될 예정
- `firebase-config.js`의 값들은 Firebase 콘솔에서 프로젝트 생성 후 사용자가 직접 발급받아 채워 넣어야 함 (Claude가 임의로 만들 수 없는 값)

## 7. 상태 관리 (app.js / auth.js 내부 구조 개략)

**auth.js** — `auth.onAuthStateChanged()`로 이미 로그인된 세션이면 app.html로 자동 이동. 로그인/회원가입/구글 로그인은 각 버튼의 이벤트 리스너 안에서 `auth.signInWithEmailAndPassword()` / `auth.createUserWithEmailAndPassword()` / `auth.signInWithPopup()`을 직접 호출 (별도 래퍼 함수로 감싸지 않음). 실패 시 `showAuthError(err)`가 Firebase 에러 코드를 한글 메시지로 변환

**app.js**
- `currentUser`: 현재 로그인한 Firebase 사용자 (없으면 `auth.onAuthStateChanged()`에서 `index.html`로 리다이렉트, 있으면 `initApp()` 실행)
- `schedules` / `categories`: Firestore `users/{uid}/schedules`, `users/{uid}/categories`를 `onSnapshot`으로 구독해 실시간 반영되는 배열
- `state.currentView`: `'calendar' | 'list'` (기기별 UI 설정이라 localStorage에 저장)
- `state.currentMonth`, `state.selectedDate`, `state.categoryFilter`, `state.searchQuery`
- 일정 CRUD: `addSchedule(data)` / `updateSchedule(id, changes)` / `deleteSchedule(id)` / `addCategory(data)`가 Firestore를 직접 호출 (성공 시 별도 재렌더링 호출 불필요 — `onSnapshot`이 감지해서 자동으로 `renderAll()`을 실행함). 폼 저장/삭제/JSON 가져오기가 모두 이 함수들만 거치도록 통일해서, 나중에 자연어 일정 등록(AI)이나 그룹 동기화 로직이 추가돼도 이 함수들만 호출하면 되게 함
- 첫 로그인 시 `categories` 스냅샷이 비어있으면 `seedDefaultCategories()`가 기본 카테고리 4개를 자동 생성
- 렌더링: `renderCalendar()`, `renderList()`, `openModal(schedule?, occurrenceDate?)`, `expandRecurring(schedule, rangeStart, rangeEnd)`
- (미구현) `checkAlarms()`: 브라우저 알림 기능은 아직 범위 밖 ([향후 확장](#9-향후-확장-가능-항목-이번-범위-제외) 참고)

## 8. 실행 시 상태 메시지 (한글 표시, CLAUDE.md 규칙 준수)

- 로그인 성공 → "로그인되었습니다"
- 회원가입 성공 → "회원가입이 완료되었습니다"
- 로그인 실패(비밀번호 오류 등) → "이메일 또는 비밀번호가 올바르지 않습니다"
- 이미 가입된 이메일로 회원가입 시도 → "이미 가입된 이메일입니다"
- 로그아웃 → "로그아웃되었습니다"
- 저장 완료 → "일정이 저장되었습니다"
- 삭제 완료 → "일정이 삭제되었습니다"
- 알림 권한 거부 시 → "알림 권한이 거부되어 알림 기능을 사용할 수 없습니다"
- 네트워크 오류/저장 실패 → "일정 저장에 실패했습니다. 네트워크를 확인해주세요"

## 9. 향후 확장 가능 항목 (이번 범위 제외)

### 9.1 기본 확장

- 그룹 만들기, 초대, 일정 공유, 개인일정 그룹일정 선택 기능
- 그룹일정은 그룹에게 공유, 개인일정은 옵션으로 공유 선택

### 9.2 개인용 AI 기능

- **자연어 일정 등록**: "다음주 화요일 3시에 김대리랑 미팅"처럼 입력하면 자동 파싱해서 일정으로 등록 (이제 기본기로 취급)
- **습관 방어(habit defense)**: 집중시간·운동 등 반복 블록을 미리 잡아두고, 다른 일정이 그 시간을 침범하면 자동으로 대안 시간을 제안 (Reclaim.ai 방식)
- **마감일 기반 자동 재배치**: 할일에 마감일만 지정하면 캘린더 빈 시간에 작업 시간을 자동으로 끼워 넣음 (Motion 방식)
- 일정 충돌 감지 후 대안 시간 제안
- 이동시간/교통상황을 고려한 스마트 알림
- 이메일·메신저 대화에서 일정 정보를 추출해 자동으로 캘린더 반영
- 회의 녹음 요약 및 액션아이템 자동 추출

### 9.3 그룹/팀용 AI 기능 (경쟁 차별화 포인트)

- 여러 참석자의 가용시간을 자동으로 교집합 계산해 최적 회의시간 제안 (필수 기능으로 간주)
- 우선순위 가중치 반영 (예: 대표 일정 우선, 특정 참석자 필수 지정 등)
- **회의 클러스터링**: 팀 회의를 한쪽으로 몰아서 집중시간 블록을 확보 (Clockwise 방식 — 현재 마땅한 대체재가 적어 눈여겨볼 만함)
- 회의록 자동 생성 후 담당자별 액션아이템 자동 배정
- 팀원별 일정 밀도를 시각화해 과부하를 경고하는 워크로드 밸런싱
- "이번주 언제 회의 가능해?" 같은 질문에 챗봇이 자동 응답

### 9.4 SI 프로젝트 관리용 확장 (이 앱을 프로젝트 일정관리로 확장할 경우)

- 마일스톤/투입인력 일정과 캘린더 동기화 → 특정 인력에게 일정이 몰려 마일스톤 위험도가 올라가면 지연 리스크 조기 경보
- 고객사 미팅 일정 조율 자동화 + 미팅 전 관련 이슈·이전 회의록을 브리핑 자료로 자동 정리
- 리소스 투입 계획(간트차트)과 실제 캘린더 사용 패턴을 비교해 "계획 대비 실투입 시간" 리포트 자동 생성

## 10. 변경 이력

설계가 바뀔 때마다 날짜와 함께 기록. 최신 항목이 위로 오도록 추가.

- **2026-08-19**: 미리보기(가짜 로그인 + 메모리 데이터)를 실제 Firebase Authentication + Firestore 연동으로 교체 — `auth.js`가 실제 이메일/비밀번호·구글 로그인을 처리하고, `app.js`는 일정/카테고리를 Firestore 실시간 구독(`onSnapshot`)으로 불러와 렌더링. `addSchedule`/`updateSchedule`/`deleteSchedule`/`addCategory`가 로컬 배열 대신 Firestore를 직접 호출하도록 변경. 최신 모듈형 SDK 대신 호환(compat) SDK를 채택해 `file://`로 직접 여는 기존 방식을 유지함 (알림/PWA는 이번 범위 밖)
- **2026-08-19**: 향후 그룹/AI 기능 확장 대비 리팩토링 — 일정 생성/수정/삭제를 `addSchedule()`/`updateSchedule()`/`deleteSchedule()`로 중앙화(폼 저장·삭제·JSON 가져오기가 모두 이 함수들을 거침), 일정 데이터에 `ownerId`/`groupId` 예약 필드 추가, 그룹 기능이 지금의 Firestore 구조(`users/{uid}/schedules`)와 안 맞는다는 점을 데이터 모델 섹션에 명시
- **2026-08-19**: JSON 내보내기/가져오기를 헤더 아이콘 버튼 즉시 실행 방식에서, 전용 "일정 데이터 관리" 팝업 안의 버튼으로 변경 (내보내기/가져오기 버튼을 한 곳에 모음)
- **2026-08-19**: 향후 확장 항목이었던 4가지를 이번 범위로 승격해 구현 — 일정 검색(제목/메모/카테고리명), JSON 내보내기/가져오기(병합 방식), 다크 모드(헤더 토글, localStorage 저장), 드래그 앤 드롭 날짜 이동(반복 없는 일정만)
- **2026-08-19**: `index.html`을 로그인 화면 전용으로 남기고, 앱 본 화면(헤더/툴바/캘린더·리스트/모달)을 `app.html`로 분리 — 로그인 성공 시 `app.html`로 이동, 로그아웃 시 `index.html`로 복귀. JS도 `auth.js`(로그인) / `app.js`(나머지 전체)로 함께 분리, 기존 `main.js`는 제거
- **2026-08-19**: 향후 확장 항목에 개인용/팀용 AI 기능(자연어 일정 등록, 습관 방어, 마감일 기반 자동 재배치, 팀 가용시간 교집합, 회의 클러스터링 등)과 SI 프로젝트 관리용 확장(마일스톤 리스크 경보, 미팅 브리핑 자동화, 투입시간 리포트) 정리해서 추가
- **2026-08-19**: (버그 수정) Nager.Date가 대체공휴일을 원래 명칭으로만 표시하던 문제 수정 — 양력 고정 공휴일의 원래 날짜와 비교해 다르면 "대체공휴일(광복절)"처럼 이름 보정
- **2026-08-19**: 일정 모달의 날짜 입력을 브라우저 기본 달력 대신 앱 디자인과 통일된 커스텀 미니 달력 팝업으로 변경 (요일 색상·공휴일 표시도 동일하게 반영)
- **2026-08-19**: (버그 수정) 날짜를 문자열로 바꿀 때 `toISOString()`(UTC 기준)을 쓰던 게 한국(UTC+9)에서 하루씩 밀리는 원인이었음 — 공휴일 표시뿐 아니라 캘린더 날짜 매칭, "오늘" 판정, 반복 일정 생성, 지난 일정 판정 등 전체에 영향 주던 문제라 로컬 날짜 기준 `toDateStr()` 헬퍼로 전부 교체
- **2026-08-19**: 캘린더에 토요일=파랑/일요일=빨강 요일 색상 적용, 대한민국 공휴일을 Nager.Date API로 조회해 빨강 표시 + 공휴일명 라벨 추가 (네이버는 공개 공휴일 API가 없어 대체재로 결정, [1.3](#13-공휴일-데이터-소스-결정-2026-08-19) 참고)
- **2026-08-19**: 완료 여부를 단일 `done` 불리언에서 회차별 `doneDates` 배열로 전환 — 반복 일정의 완료 표시가 한 회차에만 적용되도록 변경, 반복 일정 전용 "전체 완료"(및 "전체 완료 해제") 버튼 추가
- **2026-08-19**: 새 일정 추가 시 반복 설정 기본값이 "반복 없음"임을 코드에서 명시적으로 보장하도록 확인
- **2026-08-19**: 반복 종료일 기본값을 "무한"으로 변경, 1주일/한달/1년 프리셋 버튼 추가 (직접 날짜 선택도 계속 가능)
- **2026-08-19**: 모달의 "완료 여부"를 읽기 전용 모드 잠금 대상에서 제외 — "수정" 버튼을 누르지 않아도 언제든 확인 팝업을 거쳐 바로 토글 가능하게 함
- **2026-08-19**: 일정 모달의 "완료 여부" 필드를 맨 위로 이동, 리스트 카드의 완료 체크박스는 클릭 즉시가 아니라 확인 팝업을 거친 후에만 토글되도록 변경
- **2026-08-19**: 기존 일정 클릭 시 모달이 읽기 전용(보기) 모드로 열리도록 변경, "수정" 버튼을 눌러야 편집 가능하게 함 (실수로 바로 삭제/수정되는 것 방지)
- **2026-08-19**: 일정에 완료 여부(`done`) 필드 추가, 완료 시 취소선+흐리게 표시, 리스트 카드에서 바로 토글 가능
- **2026-08-19**: (복구) 툴바 버튼들을 중앙 정렬로 변경, 일정 모달에 페이드/확대 등장 애니메이션 추가, 일정 칩/카드 호버 시 요약 미리보기 툴팁 추가 — 편집기 저장 충돌로 문서에서 한 번 유실되었다가 다시 반영함
- **2026-08-19**: 캘린더 뷰에서 일정 칩을 클릭하면 날짜 상세 패널을 거치지 않고 바로 수정 모달이 열리도록 변경
- **2026-08-19**: `testapp/`을 my-portfolio 모노레포에서 분리해 별도 저장소 `my-schedule-app`으로 이전
- **2026-08-19**: 일정에 우선순위(낮음/보통/높음, 레벨별 고정 색상) 필드 추가
- **2026-08-19**: 일정 모달의 카테고리 선택을 검색형 콤보박스로 변경
- **2026-08-19**: 로그인 방식에 구글 로그인 추가, 최근 로그인 방법을 localStorage에 기록해 배지로 표시
- **2026-08-19**: 반복 설정에 "주중(월~금)"과 "요일 선택(custom)" 옵션 추가, `customDays` 필드 도입
- **2026-08-19**: 저장소를 `localStorage` 단독 방식에서 Firebase(Firestore + Authentication)로 변경, PWA 안드로이드 배포 방향(1단계 웹+PWA, 2단계 필요시 Capacitor/TWA) 확정
- **2026-08-19**: 최초 설계서 작성 — 캘린더/리스트 뷰, 일정 CRUD, 반복 일정, 알림 기본 구조
